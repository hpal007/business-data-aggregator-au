# Example environment variables for docker-compose (copy to .env and edit before use)
# See docker-compose.yaml for usage and defaults

# Project directory (on host) - default is the repo root
#AIRFLOW_PROJ_DIR=.

# UID used inside containers to set ownership of mounted volumes (recommended: your host user id)
# Obtain with `id -u` on mac/linux
#AIRFLOW_UID=50000

# Extra pip requirements to install at container start (use only for quick dev checks)
#_PIP_ADDITIONAL_REQUIREMENTS=

# Airflow web UI default credentials created at init
#_AIRFLOW_WWW_USER_USERNAME=airflow
#_AIRFLOW_WWW_USER_PASSWORD=airflow

# Path to this env file (optional override when calling docker-compose)
#ENV_FILE_PATH=.env
